# Claude Code installs a plugin's dependencies with `npm ci --ignore-scripts`, which
# would otherwise pull jsdom and c8 into every user's plugin cache and risk the
# 60-second install cap. Nothing at runtime needs them.
omit=dev
